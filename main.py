# #Phase 2: Data Loading & Initial Exploration
# Load the Dataset:

# Write a Python script (e.g., data_loader.py or directly in main.py) to load your dataset into a Pandas DataFrame.
# Handle different file formats (e.g., CSV, TSV).
# Example for CSV: df = pd.read_csv('data/sms_spam_collection.csv', encoding='latin-1') (encoding often needed for text datasets).